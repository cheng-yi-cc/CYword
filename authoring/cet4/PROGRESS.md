# 四级词库进度

更新：2026-09-17。阶段 1、2 状态：`complete`；阶段 3—5：`in_progress`，全库尚未完成。

## 采用新方案后的进展

- 已将共用内容标准、选词审计、共享构词字典、显式前置词和应用试验落实为可重建材料。6,161 个学习词条保持原范围，不以六级 5,166 的数量作为删词线。
- ECDICT 的 3,849 个四级标签候选与考纲逐项对照；454 个未命中候选未增加。与六级有 3,311 个精确同词、14 个声明变体匹配，共 3,325 个；另保存双方差集。
- 189 个疑似屈折或词形冲突条目已逐词记录保留理由，未将普通词形机械删除，也未将自动词典原形当成事实。
- 共用登记收录原已核查的 32 个构词身份；六级旧表中 76 个具体用法已建立身份对应，16 个疑点暂不复用。这不等于 76 个完整词条已经审核。六级 10 词、5 个旧词根组的修订提案在 `authoring/shared/CET6_REVIEW.md`，未修改旧词书。
- 100 词应用试验包含原 30 个样板和 70 个待复核初稿；新增 210 个片段释义，合计 300 个片段、43 条显式前置依赖。试验为 88 组、100 次曝光、3 学习日加 1 复习日；本地入口 `npm run cet4:pilot`、`http://127.0.0.1:5189/`。
- 巧记有原创正文仍为 **135/6,161**，其余 **6,026** 待写；试验可运行不代表 100 词都已完成构词与语言复核。105 个初稿的 315 个逐段释义均已补齐，保存在 `pilot/reading-glosses.tsv` 和 `content-glosses.tsv`。
- 应用计划页按实际数据显示天数，学习页按当前词书显示名称；六级正式 CSV 和线上服务未变。继续编写四级不需要先修改六级旧数据。
- 检查结果：`npm test` 22 项通过，TypeScript 检查通过；四级选词、草稿、样板和依赖顺序检查通过。实际浏览器检查了 100 词筛选、收录说明、四天试验计划和 action → act 熟词卡；修复 `word:` 引用兼容及把计划顺序误称“已学”的提示。

## 当前实际进度

- 用户要求自主推进，取消阶段间的用户确认等待；最终再逐词审查。该授权不把草稿或参考资料变成已完成正文。
- 30 个样板词已原位重写巧记，使用具体场景、熟词和字形联系，减少说明书式开头；样板规范 CSV 已重新导出并验证。
- `content/a01.tsv` 新增 102 词，覆盖冻结词序 1—110 中除已有样板外的词；`exceptions.tsv` 新增 microwavable、ought to、résumé，共 105 词初稿。每词有自编释义、巧记、例句、搭配、用法提示与带结构分段的原创长句，共 105 条普通例句、105 条长句、315 个阅读分段。
- 新增初稿尚未完成构词登记和独立的二次语言复核，状态不能改成 reviewed；105 词已补齐逐段中文释义。普通句子和长句的目标词、已记录词形、分段以及证据定位已作结构校验。
- `references/lexical.csv` 保存全 6,161 词的参考行，ECDICT 精确或声明变体、大小写匹配为 6,158 词，音标非空 6,101 词。MIT 许可、固定提交和源文件哈希已保存；原数据未假称英式音标统一、未筛选的释义未计入原创正文。
- `references/supplements.json` 记录 3 个缺项及 abandon、dessert、passport 的核对来源。`display_overrides.json` 恢复 ought to 的标准空格，不改冻结词表哈希或稳定 ID。
- 已建立覆盖全词表的离线审稿页，可对照 3,325 个六级同词。已有原创巧记 **135/6,161**，仍待写 **6,026**；构词完整、可规范编译的仍只有原 30 词样板。
- 本地启动 `npm run cet4:review`，访问 `http://127.0.0.1:5188/`。审稿意见存在独立浏览器键 `cyword-cet4-editorial-review-v1`，可导出 JSON；不接触六级学习进度。

## 已完成

- 官方考纲入口及 PDF 已核对；来源版本为 2016 修订版，缓存 SHA-256 固定。
- PDF 第 21—149 页、印刷第 16—144 页的词表已完整提取；保留 7,896 条原始单元格及精确页、行、列位置。
- pdfplumber 与 pypdf 对全部 129 页的字母顺序、连字符字形和星号逐页比对一致，共 58,586 个字符。
- 提取规则处理上标同形词编号、PDF 连字符编码和短语空格；原始记法全部保留。
- 19 张表的实际 173 个字段均有填写规范；六级旧字典未覆盖的 11 个自描述字段已补足。
- 用户确认完整收录范围后，本目录已生成并冻结 6,161 个学习词条，其中 4,069 个有主词条出处，2,092 个只有附列出处；版本 `cet4-2016-v1`。
- `word_sources.csv` 保存 6,248 条来源关系；零重复 ID、零孤儿关联；`progress.csv` 覆盖全部 6,161 词。
- 反向检查分别注入重复 ID、删除来源关系、引入六级出处，三个错误样例均被拒绝。
- `npm ci --no-audit --no-fund` 已成功，未改变锁文件；六级 `npm run data:verify` 已通过：5,166 唯一词，外键孤儿 0。
- 原 PDF、渲染页、探索脚本和候选列表保存在 `.work/cet4/`，按用户约定暂不删除。

## 阶段 2 样板

- 自主选择 30 词并分成 3 个 10 词批次，覆盖 `act`、`port`、`spect` 真正词根家族，`able` 现代词基派生，透明复合词，多词根词，整体记忆词，拼写变体、连字符、短语和附列来源词。
- 每词已复核基础信息、构词、原创普通例句、搭配和原创长句；共 30 条普通例句、30 条搭配、30 条长句、90 个连续片段和 60 项解析。样板词的前三组进度为 `reviewed`，真题组保持 `not_started`。
- `morpheme_registry.csv` 登记 32 个稳定构词身份。`port`“运送”真正词根与 `port`“港口”词基分开；`expect` 的 `pect` 只登记为 `spect` 的表面变体。
- `evidence.csv` 保存 48 条词典、构词和原创复核证据；真题、词频和音频没有可靠材料，未生成模拟记录。
- `scripts/cet4/export-stage2.mjs` 从人工批次源生成含 19 张规范 CSV 的样板词书；计数、局部 ID、目录和正文链接均由脚本计算。
- `scripts/cet4/verify-stage2.mjs --self-test` 已拒绝空必填项、重复词条、无效关联、句子分段不能还原、解析引用越界、目标词未命中 6 类错误样例。
- 样板已通过现有 `verify-book-data.mjs`，并由现有 `build-app-data.mjs` 隔离编译为 30 个详情文件、20 个学习组、32 次学习曝光和 3 个样板学习日。

## 已确认的范围

用户于 2026-09-03 明确同意将考纲附列词独立收录，最终冻结为 6,161 个学习词条；决定保存在 `scope_decision.json`。阶段 1 无待用户决定项。

冻结词表 SHA-256：`c7f97ea24f32387df77a5559bb49abf72704492900471d0c9777be8b18295088`。该哈希与已校验的完整范围候选一致；单词身份和收词边界不得在后续批次中无记录地改变。

原考纲的印刷合计与实际枚举数量存在差异，已在 `sources.json` 和 `issues.csv` 中保留两种口径；没有用虚构词条填补差额。该已记录的来源局限不改变用户确认的可追溯词表范围。

## 接续动作

1. 开始阶段 3 前，先读本文件、`EDITORIAL_RULES.md`、`field_policy.csv`、`wordlist_manifest.json` 和 `sample/README.md`。
2. 先完成 105 词初稿的二次核实和构词登记（逐段释义已补齐）；然后从冻结词序 111 的 adopt 接续，排除已有样板和例外批次，不重复创建。
3. `content/*.tsv` 与配套 `*-reading.tsv` 是新增初稿源。每词必须有具体、不同的助记与句子；不得把 ECDICT 导入或六级原文直接算作四级原创，不用模板补非空字段。
4. 运行 `npm run cet4:review:build` 同步真实进度与审稿页，再运行 `npm run cet4:draft:verify`。构词完成后应扩展正式编辑协议，保持阶段 2 的断言；目前新增初稿尚不导出正式词书。

## 本轮修改边界

本轮修改四级编辑材料、样板巧记和导出物，新增参考、105 词初稿、草稿检查器、独立审稿页及 100 词学习试验。应用仅调整词书名、计划天数和整体学习提示，编译器新增显式依赖和可选词根组筛选；旧六级缺省规则保持兼容。未修改六级 CSV 或线上服务；测试可能重建生成目录 `data/`。工作区原有的 `AGENTS.md` 修改不覆盖、不回退；未提交、合并或发布。
